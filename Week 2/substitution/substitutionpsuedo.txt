the conditions are easy to specify. they are a mix of the previous problems.



There is one condition that is difficult to check: the key can't have two
equal characters.
I start creating a loop of the string to check each character but Idk how to
compare each of them. There are two equal chars when one of them is repeated two or
more times.
Each of the letters in the alphabet could be repeated each time in upper and lower.
There has to be different values.
To check this I check if two subsequent values are equals




key: m n b v c x z l k j h g f d s a p o i u y t r e w q

plai: b d

ciph: n v


1. The difference (ASCII code) between the plain and the code is a number
2. if I add this number to the plain I obtain the ciph


cipher = posizione in ordine alfabetico del plain

char cipher b = key[1]
char cipher d = key[3]


The last function non-improved (design) so easier to read to understand the code:



//Function used to encipher the plaintext (k stands for key)
char cyphertext (string k, char plain)
{
	//If the input is uppercase
	if (isupper(plain))
	{
		//I assign to the char the (plain-65)th position of the key, e.g A - 65 = 0, so the first position of the key (in the array)
		char upper_cipher = k[plain - 65];
		//If the char in this position of the key is lower
		if (islower(upper_cipher))
		{
			//I use to upper, because the ciphertext has to have the same "dimension" (upper or lower), in this case upper because the plain is upper
			char upper_key = toupper(upper_cipher);
			return upper_key;
		}
		//If the key is upper I return the value as assigned to the char
		return upper_cipher;
	}
	//The same here, but the opposite
	else if (islower(plain))
	{
		char lower_cipher = k[plain - 97];
		if (isupper(lower_cipher))
		{
			char lower_key = tolower(lower_cipher);
			return lower_key;
		}
		return lower_cipher;
	}
	//If the char of the plaintext is not a letter I return it as it is
	else if (!isalpha(plain))
	{
 		return plain;
	}
	return 0;











    bool same_chars(string key)
{
    int counter = 0;
    for (int j = 0, n = strlen(key); j < n; j++)
    {
        for (int i = 0; i < n; i++)
        {
            if ((toupper(key[j]) == toupper(key[i])))
            {
                counter++;
                if (counter >= 27)
                {
                return false;
                }
            }
        }
    }
    return true;
}