#include <cs50.h>
#include <stdio.h>

int count_letters(string text);

int main(void)
{
	string text = get_string("Text: ");
	printf("Text: %s\n", text);

	int count_letters(string text);
	printf("Number of letters: %i\n", count_letters);

}


int count_letters(string text)
{
	
}