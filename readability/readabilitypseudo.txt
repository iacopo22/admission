
Now I start promping the user for a string (the text to analyse)

To count the number of letters I have to use the array. The text is just a string, so it's an array of characters.
I use a for loop to check each character and if it's a space, a number, a symbol or any other types, I don't count it,
else I count. (view if statement in the for loop in the code)


To count the number of words I have to check if there are spaces. If I have "Mario", after Mario there will be a space,
so 1 word. If I have Mario play, I have 2 spaces, one after Mario and one after play, so 2 words.




test con condizioni non richieste, lo tengo buono per una successiva verifica:
	int words = 1;
	for (int j = 0, n = strlen(text); j < n; j++)
	{
		if (isalpha(text[0]))
		{
			if (text[j] == 32 && isalpha(text[j++]))
			{
				words++;
			}
		}
		else (text[0] == 32)
		{
			break;
		}
	}
	return words;
