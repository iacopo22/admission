#include <cs50.h>
#include <stdio.h>

int triangle(int a, int b, int c);

int main(void)
{
	int x = get_int("One of the side: ");
	int y = get_int("One of the side: ");
	int z = get_int("One of the side: ");

	

}


int triangle(int x, int y, int z)
{
	if(a + b > c || a + c > b || c + b > a)
	{

	}
}